/*package Tests;

import android.net.Uri;
import android.support.test.rule.ActivityTestRule;

import com.example.chadlohrli.myapplication.FlashBackActivity;
import com.example.chadlohrli.myapplication.SongDownloader;

import org.junit.Before;
import org.junit.Rule;
import org.junit.Test;



public class SongDownloaderTest {
    FlashBackActivity activity;
    SongDownloader songDownloader;
    String[] addressArray = {"https://soundbible.com/grab.php?id=2200&type=mp3"};
    @Rule
    public ActivityTestRule<FlashBackActivity> flashBackActivity = new ActivityTestRule<FlashBackActivity>(FlashBackActivity.class);

    @Before
    public void setup() {
        activity = flashBackActivity.getActivity();
        songDownloader = new SongDownloader(addressArray, flashBackActivity.getActivity().getApplicationContext());
    };
}*/
